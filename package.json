{
  "name": "movie",
  "version": "1.0.0",
  "description": "movie api",
  "main": "server.js",
  "engines": {
    "node": "v12.13.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": " nodemon server.js",
    "heroku-postbuild":"cd client && npm install && npm install --only=dev --shrinkwrap && npm run build",
    "dev": "dev node server.js"
  },
  "author": "ifeanyi",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.8.5",
    "morgan": "^1.9.1",
    "nodemon": "^2.0.2",
    "request": "^2.88.0"
  }
  
}
